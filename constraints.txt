# Constraints file for Race Monitor package
# This file defines version constraints to ensure compatibility

# NumPy compatibility constraint
# tf_transformations requires NumPy < 2.0 due to deprecated np.float usage
numpy>=1.21.0,<2.0.0

# transforms3d compatibility
# Ensure we have a version that works with our NumPy constraint
# Must use pip version, not system package
transforms3d>=0.4.1,<1.0.0

# EVO trajectory evaluation constraints
# Pin to known working versions
evo>=1.26.0,<2.0.0

# Core scientific computing stack
scipy>=1.9.0,<2.0.0
pandas>=1.5.0,<3.0.0
matplotlib>=3.5.0,<4.0.0

# Ensure compatible pytest versions
pytest>=7.0.0,<9.0.0
pytest-cov>=4.0.0,<6.0.0